%% NODE POSITIONS
nodes = [0 0 0;
         1 0 0];

%% ELEMENT CONNECTIVITY
elements = [1 2];

%% NODE PROPERTIES
nprops(1).fix = true;
nprops(2).fix_orien = true;
nprops(2).force_initial = [-1 0 0]; %initial load 1N 

%% ELEMENT PROPERTIES
eprops(1).elems = [1];
eprops(1).emod = 210e9;
eprops(1).smod = 70e9;
eprops(1).dens = 7800;
eprops(1).dim = [0.05 0.05];
eprops(1).cshape = 'rect';
eprops(1).flex = [3 4 5 6];
eprops(1).orien = [0 0 1];
eprops(1).nbeams = 10;

%% RELEASES
rls = [];

%% OPTIONAL
opt.calcbuck = true;

%% DO SIMULATION
out = spacarlight(nodes,elements,nprops,eprops,rls,opt);